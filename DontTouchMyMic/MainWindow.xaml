<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DontTouchMyMic.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DontTouchMyMic"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:tb="using:H.NotifyIcon"
    Activated="Window_Activated">
    <Grid>
        <tb:TaskbarIcon x:Name="AppTaskbarIcon"
            LeftClickCommand="{x:Bind ToggleWindowFromTrayCommand}"
            RightClickCommand="{x:Bind OpenTrayMenuCommand}"
            NoLeftClickDelay="True"
            IconSource="/Assets/TrayIcon.White.ico"
            />

        <Frame x:Name="ContentFrame" Opacity="1">
            <Frame.RenderTransform>
                <TranslateTransform x:Name="ContentFrameTranslateTransform" />
            </Frame.RenderTransform>
        </Frame>
    </Grid>
</Window>
