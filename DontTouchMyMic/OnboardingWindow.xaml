<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DontTouchMyMic.OnboardingWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid x:Name="RootGrid" Background="Transparent" ActualThemeChanged="RootGrid_ActualThemeChanged">
        <Grid RowDefinitions="32,*,Auto">
            <Grid x:Name="TitleBarDragRegion" Grid.Row="0" />

            <Grid x:Name="PageViewport" Grid.Row="1" Padding="24,10,24,18">
                <Grid x:Name="PageOneContent" RowDefinitions="Auto,Auto,Auto,Auto,Auto,*" Opacity="1">
                    <Grid.RenderTransform>
                        <TranslateTransform />
                    </Grid.RenderTransform>
                    <TextBlock
                        Text="Thanks for installing Don't touch my mic!"
                        TextWrapping="WrapWholeWords"
                        FontSize="28"
                        FontWeight="SemiBold"
                        Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                    <TextBlock
                        Grid.Row="1"
                        Margin="0,8,0,0"
                        Text="Let's quickly walk through the basics."
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <TextBlock
                        Grid.Row="2"
                        Margin="0,18,0,0"
                        Text="Choose if Don't touch my mic should launch when you sign in."
                        TextWrapping="WrapWholeWords"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

                    <CheckBox
                        x:Name="RunOnStartupCheckBox"
                        Grid.Row="3"
                        Margin="0,10,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        IsChecked="True"
                        Content="Run on startup" />

                    <InfoBar
                        Grid.Row="4"
                        Margin="0,18,0,0"
                        IsClosable="False"
                        IsOpen="True"
                        Severity="Informational"
                        Message="While Don't touch my mic is running, your microphone can not be changed from outside the app." />
                </Grid>

                <Grid x:Name="PageTwoContent" Visibility="Collapsed" RowDefinitions="Auto,Auto,*" Opacity="1">
                    <Grid.RenderTransform>
                        <TranslateTransform />
                    </Grid.RenderTransform>
                    <TextBlock
                        Text="Move the tray icon into the Taskbar"
                        TextWrapping="WrapWholeWords"
                        FontSize="28"
                        FontWeight="SemiBold"
                        Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                    <Border
                        x:Name="VideoContainerBorder"
                        Grid.Row="2"
                        Margin="0,22,0,0"
                        VerticalAlignment="Top"
                        CornerRadius="8"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        SizeChanged="VideoContainerBorder_SizeChanged"
                        Background="{ThemeResource LayerFillColorDefaultBrush}">
                        <MediaPlayerElement
                            x:Name="OnboardingVideoPlayer"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Stretch="Uniform"
                            AreTransportControlsEnabled="False"
                            AutoPlay="False"
                            IsHitTestVisible="False" />
                    </Border>
                </Grid>

            </Grid>

            <Border
                Grid.Row="2"
                Padding="20,14"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0"
                Background="{ThemeResource LayerFillColorDefaultBrush}">
                <Grid>
                    <Button
                        x:Name="BackButton"
                        Padding="16,8"
                        Click="BackButton_Click"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE72B;" FontSize="14" />
                            <TextBlock Text="Back" />
                        </StackPanel>
                    </Button>

                    <Button
                        x:Name="NextButton"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right"
                        Padding="20,8"
                        Style="{StaticResource AccentButtonStyle}"
                        Click="NextButton_Click">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock x:Name="NextButtonTextBlock" Text="Next" />
                            <FontIcon x:Name="NextButtonIcon" Glyph="&#xE72A;" FontSize="14" />
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
